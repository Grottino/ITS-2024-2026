// Declares a variable to store the current age
let currentAge = 28;

// Declares a variable to store the maximum age
let maxAge = 80;

// Declares a variable to store the number of cups of coffee consumed per day
let cupsPerDay = 2;

// Calculates the total number of cups of coffee that will be consumed from the current age to the maximum age
let totalCups = (maxAge - currentAge) * 365 * cupsPerDay;